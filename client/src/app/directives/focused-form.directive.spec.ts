import { FocusedFormDirective } from './focused-form.directive';

describe('FocusedFormDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusedFormDirective();
    expect(directive).toBeTruthy();
  });
});
